services:
  - type: web
    name: galamsey-api
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "cd api && python simple_main.py"
    envVars:
      - key: PORT
        value: 10000